// Import the repository logo through Vite so the launcher UI and favicon always
// use the same source asset as the README and Tauri icon generator.
import logoUrl from "../../logo.svg?url";

export default logoUrl;
